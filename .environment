export CRAFT_DB_DRIVER="${POSTGRESQL_SCHEME}""
export CRAFT_DB_SERVER="${POSTGRESQL_HOST}"
export CRAFT_DB_PORT="${POSTGRESQL_PORT}"
export CRAFT_DB_DATABASE="${POSTGRESQL_PATH}"
export CRAFT_DB_USER="${POSTGRESQL_USERNAME}""
export CRAFT_DB_PASSWORD="${POSTGRESQL_PASSWORD}""
export CRAFT_DB_SCHEMA=public
export CRAFT_DB_TABLE_PREFIX=

export REDIS_HOSTNAME="${REDIS_HOST}"
export REDIS_PORT="${REDIS_PORT}"

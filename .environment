export CRAFT_DB_URL="${POSTGRESQL_SCHEME}://${POSTGRESQL_USERNAME}:${POSTGRESQL_PASSWORD}@${POSTGRESQL_HOST}:${POSTGRESQL_PORT}/${POSTGRESQL_PATH}"

export REDIS_HOST="${REDIS_HOST}"
export REDIS_PORT="${REDIS_PORT}"

{# @var entry \craft\elements\Entry #}

{% set section = entry.section.handle|lower %}
{% set type = entry.type.handle|lower %}

{% include [
	'entries/' ~ section ~ '/' ~ type ~ '.twig',
	'entries/' ~ type ~ '.twig',
] with {
	entry,
} only %}

{# @var entry \craft\elements\Entry #}

{% set section = entry.section.handle %}
{% set type = entry.type.handle %}

{% include [
	'pages/' ~ section ~ '/' ~ type,
	'pages/' ~ type,
] with {
	entry,
} only %}
